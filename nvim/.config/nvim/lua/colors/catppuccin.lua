local palette = require("catppuccin.palettes.macchiato")

return {
  bg = palette.base,
  fg = palette.text,
  yellow = palette.yellow,
  cyan = palette.blue,
  darkblue = palette.blue,
  green = palette.green,
  orange = palette.peach,
  violet = palette.lavender,
  magenta = palette.pink,
  blue = palette.blue,
  red = palette.red,
  polar = palette.overlay0,
  bg1 = palette.peach,
}
